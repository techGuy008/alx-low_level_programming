#include <stdio.h>
#include <string.h>
/**
 * _strlen_recursion - returns the length of a string
 * @str: string to print
 * Return: string length
 */

int _strlen_recursion(char *str)
{
return (strlen(str));
}
